version: "2.1"
services:
  notecard:
    image: "bh.cr/blues_wireless/notecard-aarch64"
    devices:
      - "/dev/i2c-1:/dev/i2c-1"
    expose:
      - "8080"
    privileged: true

  temperature:
    build: ./example-node

